/**
 * Creates a new class to hold the candidate script info
 * @class
 */
interface CandidateScriptInfo {
    // Holds the candidate script Id
    candidateScriptId: number;

    // Holds the document id
    documentId: number;
}
export = CandidateScriptInfo;
