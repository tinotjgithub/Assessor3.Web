/**
 * List of validators used for worklists. An entry should be added to this enum for all worklist validators
 */

enum WorklistValidatorList {
    liveOpen,
    atypicalOpen,
    standardisationOpen,
    practiceOpen,
    directedRemarkOpen,
    simulationOpen
}
export = WorklistValidatorList;